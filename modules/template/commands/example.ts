/**
 * Example command — registered in module index.
 * Add more commands by calling context.registerSlashCommand() or
 * context.registerSlashCommands([...]) in the module index.
 */

export const EXAMPLE_DESCRIPTION = 'Example slash command from template';
